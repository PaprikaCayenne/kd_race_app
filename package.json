{
  "name": "kd-monorepo",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "knip": "^5.58.0",
    "ts-prune": "^0.10.3"
  },
  "scripts": {
    "lint": "echo 'No root lint configured for this monorepo'",
    "test": "node --test tests/*.test.mjs",
    "build": "npm run build --prefix frontend",
    "smoke": "bash scripts/smoke_test.sh",
    "verify:all": "bash scripts/verify_all.sh"
  }
}
